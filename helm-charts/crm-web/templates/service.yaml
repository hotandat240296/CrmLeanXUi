apiVersion: v1
kind: Service
metadata:
  name: {{ include "crm-web.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "crm-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crm-web.selectorLabels" . | nindent 4 }}
